# 프로필 저장/로드 MCP 개선안 문서

## 1. 배경

현재 프로필 저장/로드 MCP는 이력서·경력기술서·포트폴리오 원문을 저장하는 기능 자체는 수행하고 있으나, 규칙 기반 파싱 결과의 품질이 낮아 **구조화 프로필을 신뢰하기 어려운 상태**입니다.

실제 테스트 결과 다음과 같은 문제가 반복적으로 확인되었습니다.

* 사용하지 않은 기술이 자동 추출됨
* 프로젝트명 또는 문맥 단어가 기술 스택으로 오인식됨
* 총 경력 기간이 정밀하게 보존되지 않음
* 사용자 교정값이 후속 재파싱에서 유지되지 않음
* 저장은 성공하지만 로드된 구조화 데이터가 원문과 불일치함
* 재파싱 결과가 이전 버전보다 나빠질 수 있음

따라서 본 MCP는 현재 기준으로는 **원문 저장소로는 usable**하나, **구조화 프로필 엔진으로는 unstable**한 상태로 판단됩니다.

---

## 2. 문제 정의

핵심 문제는 “저장이 안 된다”가 아니라 다음입니다.

> **저장은 되지만, 구조화 결과가 불안정하며, 로드 시 자동 추출 데이터를 그대로 신뢰하기 어렵다.**

즉, raw text는 보존되지만 structured data의 품질이 낮아 이후 기능들(공고 매칭, 이력서 맞춤화, 자소서 생성 등)의 입력값으로 사용하기 위험합니다.

---

## 3. 주요 문제 사례

### 3.1 잘못 추출된 기술

실제로 사용하지 않은 기술이 반복적으로 추출되었습니다.
예: Go, JavaScript, Elasticsearch
이는 기술 추출기가 **문맥 이해 없이 토큰 패턴 위주로 과잉 추출**하고 있을 가능성을 시사합니다.

### 3.2 프로젝트명/문맥 오인식

예: `WMS Android API` → `Android`를 기술 스택으로 인식
즉, 프로젝트명, 설명, 역할, 기술 항목 간 구분이 제대로 이루어지지 않고 있습니다.

### 3.3 총 경력 손실

`3년 8개월`이 `3`으로 저장되는 등, 경력 기간이 정밀하게 보존되지 않았습니다.

### 3.4 사용자 교정값 무시

사용자가 명시적으로 수정한 내용(ex. Go 안 씀, Supabase 씀)이 재파싱 시 잘못된 값이 다시 등장했습니다.

### 3.5 프로젝트 구조화 실패

입력 형식을 단순화해 재파싱한 경우 프로젝트가 아예 `0건`으로 추출되는 문제가 발생했습니다.

### 3.6 구조화 결과와 원문 간 불일치

로드 결과에서 원문은 정상 저장되어 있으나, 구조화 필드(`skills`, `projects.tech_stack`, `education`, `total_experience_years`)가 원문과 다르게 표시됩니다.

---

## 4. 저장 단계 개선안

### 4.1 규칙 기반 기술 추출 오인식 제거
* 단순 화이트리스트/사전 매칭을 넘어서 **근거 문장 기반 추출**로 전환
* 기술 추출 시 반드시 **어느 문장에서 추출됐는지 provenance** 저장
* 사용자가 명시적으로 제외한 기술은 **negative skill list**로 관리

### 4.2 프로젝트명/문맥을 기술로 오인하는 문제 해결
* `프로젝트명`, `역할`, `기술`, `성과`를 별도 필드로 강제 파싱
* `기술:` 라인에서만 1차 추출

### 4.3 섹션별 스키마 강제 필요
자유 텍스트 파싱은 보조 경로로 두고, **명시 구조 필드가 있으면 그 값을 우선**해야 합니다.

### 4.4 경력 기간 파싱 정밀도 개선
`total_experience_years`를 정수만 저장하지 말고 정밀하게 저장.
권장안: 내부 계산용: `total_experience_months = 44`, 표시용: `3년 8개월`

### 4.5 사용자 교정값 우선 반영 메커니즘 필요
프로필에 `user_confirmed_skills`, `user_rejected_skills` 필드를 추가하여 재파싱 시 **hard constraint**로 사용.

### 4.6 프로젝트 추출 안정성 개선
파싱 실패 시 전체 프로젝트를 버리지 말고 **fallback raw block**이라도 저장.

---

## 5. 로드 단계 개선안

### 5.1 raw text와 structured data를 분리
`raw_source`, `parsed_structured`, `user_confirmed` 레이어로 분리.

### 5.2 신뢰도 점수와 추출 근거 제공
각 필드에 `confidence`, `source_span`, `extraction_reason` 포함.

### 5.3 조회 시 문제 가능성 필드 표시
로드 시 validation warnings를 함께 반환.

---

## 6. 데이터 모델 개선안

### 6.1 skill을 단순 문자열이 아니라 타입화
기술 항목에 `category`, `source`, `confirmed_by_user`, `rejected_by_user`, `confidence` 부여.

### 6.2 프로젝트 기술과 전역 기술 분리 저장
주력 기술과 한정적 사용 기술 구분 가능하도록 처리.

### 6.3 canonical skill dictionary 필요
내부적으로 canonical key 운영 (예: SpringBoot / Spring Boot -> spring-boot)

---

## 7. UX 개선안

### 7.1 저장 직후 diff 확인 단계 필요
`profile_parse_resume` 직후 추가/제거 기술, 불확실 항목, 사용자 확인 필요 항목 안내.

### 7.2 수동 수정 API 필요 (최우선 도입 고려)
* `profile_update_skills`
* `profile_remove_skill`
* `profile_update_experience`
* `profile_update_project`
* `profile_confirm_fields`

### 7.3 파싱 실패 시 버전 관리 필요
이전 버전과 diff 제공 및 rollback 지원.

---

## 8. 우선순위 제안

### 8.1 최우선
1. 사용자 수정값 우선 저장
2. 잘못 추출된 기술을 제거할 수 있는 수동 수정 API 제공
3. 경력 기간을 월 단위로 저장
4. raw text / parsed / confirmed 분리

### 8.2 다음 단계
5. 프로젝트 구조화 개선
6. 기술 추출 근거와 confidence 제공
7. canonical skill taxonomy 도입

### 8.3 있으면 좋은 기능
8. 버전 관리
9. 저장 직후 diff 검토 단계
10. 불확실 필드 경고 시스템

---

## 10. 최종 진단
이 MCP는 현재 상태에서:
* **문서 원문 저장소로는 usable**
* **자동 구조화 프로필 엔진으로는 unstable**

단기적으로는 raw text 중심 운영이 필요하고, 중장기적으로는 **사용자 교정 우선, 구조화 입력 우선, 신뢰도/근거 기반 로드** 구조로 개선해야 합니다.
